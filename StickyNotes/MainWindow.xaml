<Window x:Class="StickyNotes.MainWindow" WindowState="Maximized"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StickyNotes" xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:UserControl="clr-namespace:StickyNotes"
        mc:Ignorable="d"
        Title="MainWindow" Height="425" Width="800" Background="#d2e6fa">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="800" Height="425" d:DataContext="{d:DesignInstance local:MainWindow}">
        <DockPanel>
            <StackPanel Height="420">
                <Button x:Name="AddNewNoteButton" Style="{StaticResource addNewNoteButtonStyle}" HorizontalAlignment="Left" Height="50" Width="350" Margin="0,0,10,10" Click="AddNewNoteButton_Click">
                    <DockPanel>
                        <iconPacks:PackIconBootstrapIcons Kind="PlusLg" HorizontalAlignment="Center" Height="20" Width="20" Margin="0,3,10,0" />
                        <TextBlock FontSize="20">ADD NEW STICKY NOTE</TextBlock>
                    </DockPanel>
                </Button>
                <UserControl:MainStickyNoteUC DataContext="{Binding StickyNotesList[0]}" VerticalAlignment="Top" Height="350" Width="350"></UserControl:MainStickyNoteUC>
            </StackPanel>
            <ScrollViewer MaxHeight="420" VerticalScrollBarVisibility="Auto">
                <Grid VerticalAlignment="Top" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <WrapPanel Width="420">
                        <ItemsControl ItemsSource="{Binding StickyNotesList}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type local:StickyNoteViewModel}">
                                    <UserControl:StickyNoteUC VerticalAlignment="Top" Height="200" Width="200"></UserControl:StickyNoteUC>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </WrapPanel>
                </Grid>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</Window>
